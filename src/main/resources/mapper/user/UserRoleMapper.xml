<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opsdesk.user.mapper.UserRoleMapper">

    <!-- 用户角色关系查询：只返回调用方需要的 ID 或计数。 -->
    <select id="findRoleIdsByUserId" resultType="long">
        SELECT role_id
        FROM sys_user_role
        WHERE user_id = #{userId}
          AND deleted = 0
        ORDER BY role_id
    </select>

    <select id="findUserIdsByRoleId" resultType="long">
        SELECT user_id
        FROM sys_user_role
        WHERE role_id = #{roleId}
          AND deleted = 0
        ORDER BY user_id
    </select>

    <select id="countActiveByRoleId" resultType="int">
        SELECT COUNT(1)
        FROM sys_user_role
        WHERE role_id = #{roleId}
          AND deleted = 0
    </select>

    <select id="countActive" resultType="int">
        SELECT COUNT(1)
        FROM sys_user_role
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
          AND deleted = 0
    </select>
    <insert id="insert">
        INSERT INTO sys_user_role (
          id, user_id, role_id, create_by, update_by, deleted
        )
        VALUES (
          #{id}, #{userId}, #{roleId}, #{operatorId}, #{operatorId}, 0
        )
    </insert>

    <update id="restoreDeleted">
        UPDATE sys_user_role
        SET deleted = 0,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND role_id = #{roleId}
          AND deleted = 1
        LIMIT 1
    </update>

    <update id="deactivateMissing">
        UPDATE sys_user_role
        SET deleted = 1,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND deleted = 0
        <if test="roleIds != null and roleIds.size > 0">
          AND role_id NOT IN
          <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
          </foreach>
        </if>
    </update>
</mapper>
