<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opsdesk.department.mapper.DepartmentMapper">

    <!-- 部门基础字段：组织树读取显式列字段，避免后续扩展字段自动暴露。 -->
    <sql id="DepartmentColumns">
        id, parent_id, name, leader_id, sort, enabled, create_time, update_time, create_by, update_by, deleted
    </sql>

    <select id="findById" resultType="com.opsdesk.department.entity.Department">
        SELECT <include refid="DepartmentColumns"/>
        FROM sys_department
        WHERE id = #{id}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="findEnabledById" resultType="com.opsdesk.department.entity.Department">
        SELECT <include refid="DepartmentColumns"/>
        FROM sys_department
        WHERE id = #{id}
          AND enabled = 1
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="searchTree" resultType="com.opsdesk.department.entity.Department">
        SELECT <include refid="DepartmentColumns"/>
        FROM sys_department
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
          AND name LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="enabled != null">
          AND enabled = #{enabled}
        </if>
        ORDER BY sort ASC, id ASC
    </select>

    <insert id="insert">
        INSERT INTO sys_department (
          id, parent_id, name, leader_id, sort, enabled, create_by, update_by, deleted
        )
        VALUES (
          #{id}, #{parentId}, #{name}, #{leaderId}, #{sort}, #{enabled}, #{createBy}, #{updateBy}, 0
        )
    </insert>

    <update id="update">
        UPDATE sys_department
        SET parent_id = #{parentId},
            name = #{name},
            leader_id = #{leaderId},
            sort = #{sort},
            enabled = #{enabled},
            update_by = #{updateBy},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <update id="logicalDelete">
        UPDATE sys_department
        SET deleted = 1,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="countByParentAndName" resultType="int">
        SELECT COUNT(1)
        FROM sys_department
        WHERE deleted = 0
          AND name = #{name}
        <choose>
          <when test="parentId == null">
            AND parent_id IS NULL
          </when>
          <otherwise>
            AND parent_id = #{parentId}
          </otherwise>
        </choose>
        <if test="excludeId != null">
          AND id != #{excludeId}
        </if>
    </select>

    <select id="countChildren" resultType="int">
        SELECT COUNT(1)
        FROM sys_department
        WHERE parent_id = #{parentId}
          AND deleted = 0
    </select>

    <select id="findDescendantIds" resultType="long">
        WITH RECURSIVE department_tree AS (
          SELECT id
          FROM sys_department
          WHERE parent_id = #{departmentId}
            AND deleted = 0
          UNION ALL
          SELECT d.id
          FROM sys_department d
          INNER JOIN department_tree dt ON d.parent_id = dt.id
          WHERE d.deleted = 0
        )
        SELECT id
        FROM department_tree
    </select>
</mapper>
