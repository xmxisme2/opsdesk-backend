<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opsdesk.attachment.mapper.AttachmentMapper">

    <!-- 附件基础字段：storage_path 只供后端存储读取，VO 转换时不会直接对外暴露。 -->
    <sql id="AttachmentColumns">
        a.id, a.biz_type, a.biz_id, a.temp_token, a.file_name, a.file_size, a.content_type,
        a.extension, a.previewable, a.preview_type, a.download_only, a.storage_path, a.uploader_id,
        a.create_time, a.update_time, a.create_by, a.update_by, a.deleted
    </sql>

    <insert id="insert">
        INSERT INTO ticket_attachment (
          id, biz_type, biz_id, temp_token, file_name, file_size, content_type, extension,
          previewable, preview_type, download_only, storage_path, uploader_id,
          create_by, update_by, deleted
        )
        VALUES (
          #{id}, #{bizType}, #{bizId}, #{tempToken}, #{fileName}, #{fileSize}, #{contentType}, #{extension},
          #{previewable}, #{previewType}, #{downloadOnly}, #{storagePath}, #{uploaderId},
          #{createBy}, #{updateBy}, 0
        )
    </insert>

    <select id="findById" resultType="com.opsdesk.attachment.entity.Attachment">
        SELECT <include refid="AttachmentColumns"/>,
               COALESCE(NULLIF(u.nickname, ''), NULLIF(u.username, ''), u.phone) AS uploader_name
        FROM ticket_attachment a
        LEFT JOIN sys_user u ON u.id = a.uploader_id AND u.deleted = 0
        WHERE a.id = #{id}
          AND a.deleted = 0
        LIMIT 1
    </select>

    <select id="findByBiz" resultType="com.opsdesk.attachment.entity.Attachment">
        SELECT <include refid="AttachmentColumns"/>,
               COALESCE(NULLIF(u.nickname, ''), NULLIF(u.username, ''), u.phone) AS uploader_name
        FROM ticket_attachment a
        LEFT JOIN sys_user u ON u.id = a.uploader_id AND u.deleted = 0
        WHERE a.biz_type = #{bizType}
          AND a.biz_id = #{bizId}
          AND a.deleted = 0
        ORDER BY a.create_time ASC, a.id ASC
    </select>

    <select id="findByTempToken" resultType="com.opsdesk.attachment.entity.Attachment">
        SELECT <include refid="AttachmentColumns"/>,
               COALESCE(NULLIF(u.nickname, ''), NULLIF(u.username, ''), u.phone) AS uploader_name
        FROM ticket_attachment a
        LEFT JOIN sys_user u ON u.id = a.uploader_id AND u.deleted = 0
        WHERE a.biz_type = #{bizType}
          AND a.biz_id IS NULL
          AND a.temp_token = #{tempToken}
          AND a.uploader_id = #{uploaderId}
          AND a.deleted = 0
        ORDER BY a.create_time ASC, a.id ASC
    </select>

    <update id="bindTempToBiz">
        UPDATE ticket_attachment
        SET biz_id = #{bizId},
            temp_token = NULL,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE biz_type = #{bizType}
          AND biz_id IS NULL
          AND temp_token = #{tempToken}
          AND uploader_id = #{uploaderId}
          AND deleted = 0
    </update>

    <select id="lockTicket" resultType="long">
        SELECT id
        FROM ticket
        WHERE id = #{ticketId}
          AND deleted = 0
        FOR UPDATE
    </select>

    <select id="countActiveByTicketScope" resultType="long">
        SELECT COUNT(1)
        FROM ticket_attachment a
        WHERE a.deleted = 0
          AND (
            (a.biz_type = 'TICKET' AND a.biz_id = #{ticketId})
            OR (
              a.biz_type = 'COMMENT'
              AND EXISTS (
                SELECT 1
                FROM ticket_comment c
                WHERE c.id = a.biz_id
                  AND c.ticket_id = #{ticketId}
                  AND c.deleted = 0
              )
            )
          )
    </select>

    <update id="logicalDelete">
        UPDATE ticket_attachment
        SET deleted = 1,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <select id="findCommentResource" resultType="com.opsdesk.attachment.model.AttachmentResourceInfo">
        SELECT ticket_id,
               author_id AS owner_id,
               comment_type AS status
        FROM ticket_comment
        WHERE id = #{commentId}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="findKnowledgeResource" resultType="com.opsdesk.attachment.model.AttachmentResourceInfo">
        SELECT author_id AS owner_id,
               status
        FROM knowledge_article
        WHERE id = #{articleId}
          AND deleted = 0
        LIMIT 1
    </select>
</mapper>
