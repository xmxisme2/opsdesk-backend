<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opsdesk.ticket.mapper.TicketOperationLogMapper">

    <!-- 工单操作日志字段：用于详情页时间线分页展示。 -->
    <select id="searchByTicketId" resultType="com.opsdesk.ticket.entity.TicketOperationLog">
        SELECT id, ticket_id, operation_type, from_status, to_status, operator_id,
               content, request_ip, user_agent, create_time, update_time, create_by, update_by, deleted
        FROM ticket_operation_log
        WHERE ticket_id = #{ticketId}
          AND deleted = 0
        ORDER BY create_time DESC, id DESC
    </select>
</mapper>
