<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opsdesk.role.mapper.RolePermissionMapper">

    <!-- 角色权限关系计数：用于替换权限时判断是否已有有效关联。 -->
    <select id="countActive" resultType="int">
        SELECT COUNT(1)
        FROM sys_role_permission
        WHERE role_id = #{roleId}
          AND permission_id = #{permissionId}
          AND deleted = 0
    </select>
    <update id="restoreDeleted">
        UPDATE sys_role_permission
        SET deleted = 0,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE role_id = #{roleId}
          AND permission_id = #{permissionId}
          AND deleted = 1
        LIMIT 1
    </update>

    <insert id="insert">
        INSERT INTO sys_role_permission (
          id, role_id, permission_id, create_by, update_by, deleted
        )
        VALUES (
          #{id}, #{roleId}, #{permissionId}, #{operatorId}, #{operatorId}, 0
        )
    </insert>

    <update id="deactivateMissing">
        UPDATE sys_role_permission
        SET deleted = 1,
            update_by = #{operatorId},
            update_time = CURRENT_TIMESTAMP
        WHERE role_id = #{roleId}
          AND deleted = 0
        <if test="permissionIds != null and permissionIds.size > 0">
          AND permission_id NOT IN
          <foreach collection="permissionIds" item="permissionId" open="(" separator="," close=")">
            #{permissionId}
          </foreach>
        </if>
    </update>
</mapper>
