package com.opsdesk.config;

import org.springframework.context.annotation.Configuration;

/**
 * MyBatis 基础配置入口。
 *
 * <p>当前阶段只启用 application.yml 中的通用配置，后续新增 Mapper 时按模块显式添加 @Mapper。</p>
 */
@Configuration
public class MyBatisConfig {
}

